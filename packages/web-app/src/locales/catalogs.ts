// @ts-ignore TODO: find a way to add in custom types?
import en from './en/messages'
// @ts-ignore TODO: find a way to add in custom types?
import fr from './fr/messages'

export const catalogs = { en, fr }
